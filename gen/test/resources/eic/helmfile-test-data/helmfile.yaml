repositories:
  - name: stable
    url: https://charts.helm.sh/stable

releases:
  - name: myapp-deployment
    namespace: default
    chart: ./charts/myapp-chart
    values:
      - values/values.yaml
